doctype html
html
  head
    title Список пользователей
  body
    h1 Список пользователей
    ul
      each user in users
        li
          a(href=route('user', { id: user.id }))= user.userName